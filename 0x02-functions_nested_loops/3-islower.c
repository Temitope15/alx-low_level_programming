#include "main.h"

/**
 * _islower - detects if a letter is lowercase
 * 
 * @c: character to check
 *
 * Return: 1 if @c is in lowercase else return o
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
