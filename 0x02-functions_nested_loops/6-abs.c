#include "main.h"

/**
 * int_abs - prints the absolutr value of n
 *
 * @n: the number to convert
 *
 * Return: the absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
